const config = {
    width: 480,
    height: 320,
    parent: "container",
    type: Phaser.CANVAS,
    backgroundColor: '#222'
}

new Phaser.Game(config);

function preload(){

}

function create(){

}

function update(time, delta){
    
}